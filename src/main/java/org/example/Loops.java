package org.example;

public class Loops {
    static void main () {
       /* for (int i = 1; i < 11; i++) {
            IO.println(i);
        }


        for (int i = 0; i < 17; i++) {
            IO.println(i);
        }

        //------
        int tal= 65536;

        for (int i = 65536; i >= 0 ; i>) {

            i / 2;
        }

   //.......

        int tal = 65536;

        while (tal > 2) {
            IO.println(tal);
            tal = tal / 2;
        }
        int tall = 65536;

        while (tall > 2) {
            IO.println(tall);
            tall = tall >> 1;
        }

        //ZZZZZZZZ
        int tal=  65536;
        while (tal > 2) {
            IO.println(tal);
            tal = tal / 10;
        }

        //--------
    int[] arr = {1,23,4,5,6,7};
    IO.println(arr);
    }
    static boolean trueArr () {

        return;

        //-------------------
    boolean[] svar = {true, false, true, true, false,true, true};

int resultat = antalTrue(svar);
int resultat2 = antalFalse(svar);

IO.println(resultat); // 3
IO.println(resultat2); // 3

    }
        static int antalTrue(boolean[] värden) {
            int antal = 0;

            for (int i = 0; i < värden.length; i++) {
                if (värden[i]) {
                    antal++;
                }
            }

            return antal;
        }

            static int antalFalse(boolean[] värden) {
                int antal = 0;

                for (int i = 0; i < värden.length; i++) {
                    if (!värden[i]) {
                        antal++;
                    }
                }

                return antal;

    //vilken yoga allt detta är.. jag är kass på syntax. Måste verkligen ha projekt om jag ska lära mig ordentligt.
        */

    }
    }

