package org.example;

public class Excercise5 {
    static void main() {
        /*int secret = Math.random();
        int guess = IO.readln();

        do{

            guess = Integer.parseInt(IO.readln("guess a number"));
        }
        while (guess != secret) {
            break;
        }

        int secret = nre Random().nextint(101);

        String smile = "";
        */
    }
}
